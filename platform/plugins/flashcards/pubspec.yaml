name: flashcards
description: "Flashcards Plugin"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  drift: ^2.31.0
  go_router: ^14.8.1
  file_picker: ^8.0.0
  http: ^1.2.0
  shared_ui:
    path: ../../shared/shared_ui
  shared_auth:
    path: ../../shared/shared_auth
  shared_db:
    path: ../../shared/shared_db
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0